{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "MongoDBSettings": {
    "Host": "localhost",
    "Port": "27017",
    //for mongo authentication
    //using .NET Secret Manager to store the pass word instead of putting it here as well for security reasons
    "User": "mongoadmin"
  }
}
